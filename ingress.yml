apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-hosts
spec:
  rules:
    - host: www.romajan.net
      http:
        paths:
          - backend:
              serviceName: simple-svc
              servicePort: 80