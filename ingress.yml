apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-simple
spec:
  rules:
    - http:
        paths:
          - path: /simple-spring
            pathType: Prefix
            backend:
              service:
                name: simple-svc
                port:
                  number: 80
